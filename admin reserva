<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bemorh - Tecnologia que Conecta Ideias</title>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&family=Open+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <style>
        /* Estilos COMPARTILHADOS */
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
            --accent: #e74c3c;
            --light: #ecf0f1;
            --dark: #2c3e50;
            --success: #2ecc71;
            --warning: #f39c12;
            --gray: #95a5a6;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Open Sans', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f9f9f9;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            line-height: 1.2;
            margin-bottom: 15px;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .section {
            padding: 80px 0;
        }

        .section-header {
            text-align: center;
            margin-bottom: 50px;
        }

        .section-header h2 {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 15px;
        }

        .section-header p {
            font-size: 1.1rem;
            color: var(--gray);
            max-width: 700px;
            margin: 0 auto;
        }

        .btn {
            display: inline-block;
            padding: 14px 28px;
            background-color: var(--secondary);
            color: white;
            border: none;
            border-radius: 4px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
            font-size: 1rem;
        }

        .btn:hover {
            background-color: #2980b9;
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .btn-contact {
            background-color: var(--success);
            width: 100%;
        }

        .btn-contact:hover {
            background-color: #27ae60;
        }

        /* Header/Navbar */
        header {
            background-color: white;
            box-shadow: var(--shadow);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-img {
            width: 50px;
            height: 50px;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
        }

        .logo-img img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            padding: 5px;
        }

        .logo-text {
            display: flex;
            flex-direction: column;
        }

        .logo-main {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary);
            letter-spacing: 1px;
        }

        .logo-tagline {
            font-size: 0.9rem;
            color: var(--secondary);
            font-style: italic;
            margin-top: -5px;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--primary);
            font-weight: 600;
            font-size: 1rem;
            transition: var(--transition);
            position: relative;
        }

        .nav-links a:hover {
            color: var(--secondary);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            background-color: var(--secondary);
            bottom: -5px;
            left: 0;
            transition: var(--transition);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--primary);
            cursor: pointer;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, var(--primary) 0%, #1a2530 100%);
            color: white;
            padding: 150px 0 100px;
            margin-top: 80px;
        }

        .hero-content {
            display: flex;
            align-items: center;
            gap: 50px;
        }

        .hero-text {
            flex: 1;
        }

        .hero-text h1 {
            font-size: 3.5rem;
            color: white;
            margin-bottom: 20px;
        }

        .hero-text p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .hero-image {
            flex: 1;
        }

        .hero-image img {
            width: 100%;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        /* Serviços */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .service-card {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
        }

        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .service-icon {
            width: 80px;
            height: 80px;
            background-color: rgba(52, 152, 219, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
        }

        .service-icon i {
            font-size: 2rem;
            color: var(--secondary);
        }

        .service-card h3 {
            font-size: 1.5rem;
            color: var(--primary);
        }

        /* Projetos */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .project-card {
            background-color: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        .project-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .project-image {
            height: 200px;
            overflow: hidden;
        }

        .project-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .project-card:hover .project-image img {
            transform: scale(1.1);
        }

        .project-content {
            padding: 25px;
        }

        .project-content h3 {
            font-size: 1.3rem;
            color: var(--primary);
        }

        .project-category {
            display: inline-block;
            background-color: rgba(52, 152, 219, 0.1);
            color: var(--secondary);
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            margin: 10px 0;
        }

        /* Contato */
        .contact-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 50px;
            margin-top: 40px;
        }

        .contact-info {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        .info-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 25px;
        }

        .info-icon {
            width: 50px;
            height: 50px;
            background-color: rgba(52, 152, 219, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            flex-shrink: 0;
        }

        .info-icon i {
            color: var(--secondary);
            font-size: 1.2rem;
        }

        .info-content h3 {
            font-size: 1.2rem;
            color: var(--primary);
            margin-bottom: 5px;
        }

        .contact-form {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--primary);
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-family: 'Open Sans', sans-serif;
            font-size: 1rem;
            transition: var(--transition);
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--secondary);
            box-shadow: 0 0 0 2px rgba(52, 152, 219, 0.2);
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        /* Alert Messages */
        .alert {
            padding: 12px;
            border-radius: 4px;
            margin-bottom: 20px;
            display: none;
        }

        .alert-success {
            background-color: rgba(46, 204, 113, 0.1);
            color: var(--success);
            border-left: 4px solid var(--success);
        }

        .alert-danger {
            background-color: rgba(231, 76, 60, 0.1);
            color: var(--accent);
            border-left: 4px solid var(--accent);
        }

        /* Footer */
        footer {
            background-color: var(--primary);
            color: white;
            padding: 50px 0 20px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-column h3 {
            color: white;
            font-size: 1.3rem;
            margin-bottom: 20px;
        }

        .footer-column p {
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.6;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 10px;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: white;
            padding-left: 5px;
        }

        .copyright {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.9rem;
        }

        /* Responsividade */
        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
            }

            .hero-text h1 {
                font-size: 2.8rem;
            }

            .section-header h2 {
                font-size: 2.2rem;
            }
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background-color: white;
                flex-direction: column;
                padding: 20px;
                box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            }

            .nav-links.active {
                display: flex;
            }

            .mobile-menu-btn {
                display: block;
            }

            .hero {
                padding: 120px 0 80px;
            }

            .hero-text h1 {
                font-size: 2.2rem;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .section-header h2 {
                font-size: 1.8rem;
            }
        }

        @media (max-width: 576px) {
            .hero-text h1 {
                font-size: 1.8rem;
            }

            .section {
                padding: 60px 0;
            }

            .service-card, .project-card, .contact-form, .contact-info {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Header/Navbar -->
    <header>
        <div class="container">
            <nav class="navbar">
                <div class="logo">
                    <div class="logo-img">
                        <img src="imagem.png" alt="Bemorh Logo" id="logoImage" onerror="handleLogoError(this)">
                        <div id="logoText" style="display: none; width: 100%; height: 100%; background: linear-gradient(135deg, var(--secondary), var(--primary)); color: white; font-weight: bold; font-size: 1.5rem; display: flex; align-items: center; justify-content: center;">
                            BH
                        </div>
                    </div>
                    <div class="logo-text">
                        <div class="logo-main">Bemorh</div>
                        <div class="logo-tagline" id="companyTaglineDisplay">tecnologia que conecta ideias</div>
                    </div>
                </div>
                
                <ul class="nav-links" id="navLinks">
                    <li><a href="#home">Início</a></li>
                    <li><a href="#sobre">Sobre</a></li>
                    <li><a href="#servicos">Serviços</a></li>
                    <li><a href="#projetos">Projetos</a></li>
                    <li><a href="#contato">Contato</a></li>
                    <li><a href="admin.html" class="btn" style="padding: 8px 16px; font-size: 0.9rem;">
                        <i class="fas fa-lock"></i> Admin
                    </a></li>
                </ul>
                
                <button class="mobile-menu-btn" id="mobileMenuBtn">
                    <i class="fas fa-bars"></i>
                </button>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <h1 id="companyNameDisplay">Bemorh</h1>
                    <p id="companyAboutDisplay">Nascemos da crença de que as melhores soluções surgem da conexão de ideias. Somos uma empresa de tecnologia especializada em desenvolver ferramentas virtuais que facilitam a colaboração, estimulam a inovação e transformam conceitos em realidade.</p>
                    <div style="display: flex; gap: 15px;">
                        <a href="#projetos" class="btn">Nossos Projetos</a>
                        <a href="#contato" class="btn" style="background-color: var(--success);">Fale Conosco</a>
                    </div>
                </div>
                <div class="hero-image">
                    <img src="https://images.unsplash.com/photo-1552664730-d307ca884978?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80" alt="Inovação e Tecnologia">
                </div>
            </div>
        </div>
    </section>

    <!-- Sobre -->
    <section id="sobre" class="section">
        <div class="container">
            <div class="section-header">
                <h2>Sobre Nós</h2>
                <p>Conheça mais sobre nossa missão, visão e valores</p>
            </div>
            <div style="max-width: 800px; margin: 0 auto;">
                <div style="background-color: white; padding: 40px; border-radius: 10px; box-shadow: var(--shadow);">
                    <h3 style="color: var(--primary); margin-bottom: 20px;">Nossa História</h3>
                    <p style="margin-bottom: 20px; font-size: 1.1rem; line-height: 1.8;">
                        A <strong>Bemorh</strong> surgiu em 2020 com o objetivo de conectar pessoas através da tecnologia. 
                        Percebemos que muitas empresas têm ideias brilhantes, mas falta uma estrutura para desenvolvê-las.
                    </p>
                    <p style="margin-bottom: 20px; font-size: 1.1rem; line-height: 1.8;">
                        Começamos como uma pequena startup e hoje somos referência em desenvolvimento de soluções 
                        tecnológicas que promovem colaboração e inovação.
                    </p>
                    
                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-top: 40px;">
                        <div style="text-align: center; padding: 20px; background-color: rgba(52, 152, 219, 0.05); border-radius: 10px;">
                            <div style="font-size: 2.5rem; color: var(--secondary); font-weight: bold;">3+</div>
                            <div style="font-weight: 600; color: var(--primary);">Anos de Mercado</div>
                        </div>
                        <div style="text-align: center; padding: 20px; background-color: rgba(52, 152, 219, 0.05); border-radius: 10px;">
                            <div style="font-size: 2.5rem; color: var(--secondary); font-weight: bold;" id="projectCountDisplay">20+</div>
                            <div style="font-weight: 600; color: var(--primary);">Projetos Concluídos</div>
                        </div>
                        <div style="text-align: center; padding: 20px; background-color: rgba(52, 152, 219, 0.05); border-radius: 10px;">
                            <div style="font-size: 2.5rem; color: var(--secondary); font-weight: bold;" id="clientCountDisplay">15+</div>
                            <div style="font-weight: 600; color: var(--primary);">Clientes Satisfeitos</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Serviços -->
    <section id="servicos" class="section" style="background-color: #f5f7fa;">
        <div class="container">
            <div class="section-header">
                <h2>Nossos Serviços</h2>
                <p>Soluções tecnológicas personalizadas para o seu negócio</p>
            </div>
            <div class="services-grid">
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-laptop-code"></i>
                    </div>
                    <h3>Desenvolvimento Web</h3>
                    <p>Sites, sistemas web e aplicações responsivas com as melhores tecnologias do mercado.</p>
                </div>
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h3>Apps Mobile</h3>
                    <p>Aplicativos nativos e híbridos para iOS e Android, com designs intuitivos e funcionais.</p>
                </div>
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-palette"></i>
                    </div>
                    <h3>UI/UX Design</h3>
                    <p>Design centrado no usuário, interfaces intuitivas e experiências digitais memoráveis.</p>
                </div>
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-lightbulb"></i>
                    </div>
                    <h3>Consultoria em Inovação</h3>
                    <p>Ajudo sua empresa a implementar processos inovadores e soluções tecnológicas estratégicas.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Projetos -->
    <section id="projetos" class="section">
        <div class="container">
            <div class="section-header">
                <h2>Projetos Recentes</h2>
                <p>Conheça alguns dos nossos trabalhos mais recentes</p>
            </div>
            <div class="projects-grid" id="projectsGrid">
                <!-- Projetos serão carregados dinamicamente aqui -->
                <div style="grid-column: 1 / -1; text-align: center; padding: 40px; color: var(--gray);">
                    <i class="fas fa-spinner fa-spin"></i> Carregando projetos...
                </div>
            </div>
        </div>
    </section>

    <!-- Contato -->
    <section id="contato" class="section" style="background-color: #f5f7fa;">
        <div class="container">
            <div class="section-header">
                <h2>Entre em Contato</h2>
                <p>Estamos prontos para transformar suas ideias em realidade. Entre em contato e vamos conversar sobre seu projeto.</p>
            </div>
            
            <div class="contact-container">
                <!-- Informações de Contato -->
                <div class="contact-info">
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="info-content">
                            <h3>Endereço</h3>
                            <p id="companyAddressDisplay">Av. das Inovações, 123 - Centro de Tecnologia, São Paulo - SP</p>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div class="info-content">
                            <h3>Telefone</h3>
                            <p id="companyPhoneDisplay">(11) 3456-7890</p>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="info-content">
                            <h3>Email</h3>
                            <p id="companyEmailDisplay">contato@bemorh.com.br</p>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-clock"></i>
                        </div>
                        <div class="info-content">
                            <h3>Horário de Atendimento</h3>
                            <p>Segunda a Sexta: 9h às 18h</p>
                        </div>
                    </div>
                </div>
                
                <!-- Formulário de Contato -->
                <div class="contact-form">
                    <form id="contactForm">
                        <div class="alert alert-success" id="contactSuccess" style="display: none;">
                            <i class="fas fa-check-circle"></i> Mensagem enviada com sucesso! Entraremos em contato em breve.
                        </div>
                        <div class="alert alert-danger" id="contactError" style="display: none;">
                            <i class="fas fa-exclamation-circle"></i> Erro ao enviar mensagem. Tente novamente.
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label for="contactName">Nome *</label>
                                <input type="text" id="contactName" name="name" required>
                            </div>
                            <div class="form-group">
                                <label for="contactEmail">Email *</label>
                                <input type="email" id="contactEmail" name="email" required>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="contactPhone">Telefone</label>
                            <input type="text" id="contactPhone" name="phone">
                        </div>
                        
                        <div class="form-group">
                            <label for="contactSubject">Assunto *</label>
                            <select id="contactSubject" name="subject" required>
                                <option value="">Selecione um assunto</option>
                                <option value="orcamento">Orçamento de Projeto</option>
                                <option value="duvida">Dúvida sobre Serviços</option>
                                <option value="parceria">Proposta de Parceria</option>
                                <option value="suporte">Suporte Técnico</option>
                                <option value="outro">Outro</option>
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="contactMessage">Mensagem *</label>
                            <textarea id="contactMessage" name="message" rows="5" required></textarea>
                        </div>
                        
                        <button type="submit" class="btn btn-contact" id="submitBtn">
                            <span id="submitText">Enviar Mensagem</span>
                            <span id="submitLoading" style="display: none;">
                                <i class="fas fa-spinner fa-spin"></i> Enviando...
                            </span>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>Bemorh</h3>
                    <p>Tecnologia que conecta ideias e transforma conceitos em realidade digital.</p>
                    <div style="display: flex; gap: 15px; margin-top: 20px;">
                        <a href="#" style="color: white; font-size: 1.2rem;"><i class="fab fa-facebook"></i></a>
                        <a href="#" style="color: white; font-size: 1.2rem;"><i class="fab fa-instagram"></i></a>
                        <a href="#" style="color: white; font-size: 1.2rem;"><i class="fab fa-linkedin"></i></a>
                        <a href="#" style="color: white; font-size: 1.2rem;"><i class="fab fa-twitter"></i></a>
                    </div>
                </div>
                <div class="footer-column">
                    <h3>Links Rápidos</h3>
                    <ul class="footer-links">
                        <li><a href="#home">Início</a></li>
                        <li><a href="#sobre">Sobre Nós</a></li>
                        <li><a href="#servicos">Serviços</a></li>
                        <li><a href="#projetos">Projetos</a></li>
                        <li><a href="#contato">Contato</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Serviços</h3>
                    <ul class="footer-links">
                        <li><a href="#servicos">Desenvolvimento Web</a></li>
                        <li><a href="#servicos">Apps Mobile</a></li>
                        <li><a href="#servicos">UI/UX Design</a></li>
                        <li><a href="#servicos">Consultoria</a></li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2023 Bemorh. Todos os direitos reservados.</p>
                <p style="margin-top: 5px;">
                    <a href="admin.html" style="color: rgba(255, 255, 255, 0.6); text-decoration: none;">
                        <i class="fas fa-lock"></i> Painel Administrativo
                    </a>
                </p>
            </div>
        </div>
    </footer>

    <!-- Firebase SDK (versão modular) -->
    <script type="module">
        // Import the functions you need from the SDKs you need
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
        import { 
            getFirestore, 
            collection, 
            addDoc, 
            getDocs, 
            query, 
            orderBy, 
            serverTimestamp 
        } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

        // Your web app's Firebase configuration
        const firebaseConfig = {
            apiKey: "AIzaSyAqNKa5jp5UpcTtnAF1_EUVKCXVrGZTFkA",
            authDomain: "bemorh-bc5a8.firebaseapp.com",
            projectId: "bemorh-bc5a8",
            storageBucket: "bemorh-bc5a8.firebasestorage.app",
            messagingSenderId: "313271255362",
            appId: "1:313271255362:web:167e42b830f8fdcb9188db"
        };

        // Initialize Firebase
        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);

        // Função para salvar contato no Firebase
        async function saveContactToFirebase(contactData) {
            try {
                // Adiciona timestamp do servidor
                const contactWithTimestamp = {
                    ...contactData,
                    timestamp: serverTimestamp(),
                    status: "novo",
                    read: false
                };
                
                // Remove timestamp original (substituído pelo serverTimestamp)
                delete contactWithTimestamp.date;
                
                // Salva no Firestore na coleção "contacts"
                const docRef = await addDoc(collection(db, "contacts"), contactWithTimestamp);
                
                console.log("Contato salvo com ID: ", docRef.id);
                return { success: true, id: docRef.id };
                
            } catch (error) {
                console.error("Erro ao salvar contato: ", error);
                return { success: false, error: error.message };
            }
        }

        // Função para carregar projetos do Firebase
        async function loadProjectsFromFirebase() {
            try {
                const projectsGrid = document.getElementById('projectsGrid');
                
                // Primeiro, tenta carregar do localStorage (cache)
                const cachedProjects = localStorage.getItem('bemorhSiteProjects');
                if (cachedProjects) {
                    const projects = JSON.parse(cachedProjects);
                    displayProjects(projects.filter(p => p.status === 'active'));
                }
                
                // Depois, carrega do Firebase
                const querySnapshot = await getDocs(
                    query(collection(db, "siteProjects"), orderBy("timestamp", "desc"))
                );
                
                const projects = [];
                querySnapshot.forEach((doc) => {
                    const project = doc.data();
                    project.id = doc.id;
                    projects.push(project);
                });
                
                // Atualiza o cache
                if (projects.length > 0) {
                    localStorage.setItem('bemorhSiteProjects', JSON.stringify(projects));
                    displayProjects(projects.filter(p => p.status === 'active'));
                }
                
            } catch (error) {
                console.error("Erro ao carregar projetos:", error);
                
                // Fallback para dados locais se Firebase falhar
                const localProjects = JSON.parse(localStorage.getItem('bemorhSiteProjects') || '[]');
                displayProjects(localProjects.filter(p => p.status === 'active'));
            }
        }

        // Função para exibir projetos
        function displayProjects(projects) {
            const projectsGrid = document.getElementById('projectsGrid');
            
            if (!projects || projects.length === 0) {
                projectsGrid.innerHTML = `
                    <div style="grid-column: 1 / -1; text-align: center; padding: 40px; color: var(--gray);">
                        <i class="fas fa-folder-open"></i>
                        <p style="margin-top: 10px;">Nenhum projeto disponível no momento.</p>
                        <p style="font-size: 0.9rem; margin-top: 5px;">Adicione projetos no painel administrativo.</p>
                    </div>
                `;
                return;
            }
            
            projectsGrid.innerHTML = '';
            
            // Limita a 6 projetos
            const displayProjects = projects.slice(0, 6);
            
            displayProjects.forEach(project => {
                const projectCard = document.createElement('div');
                projectCard.className = 'project-card';
                
                // Converte categoria para texto
                const categoryText = {
                    'web': 'Desenvolvimento Web',
                    'mobile': 'Apps Mobile',
                    'design': 'UI/UX Design',
                    'consulting': 'Consultoria',
                    'innovation': 'Inovação'
                }[project.category] || project.category;
                
                // Usa primeira imagem ou placeholder
                const imageUrl = project.images && project.images.length > 0 
                    ? project.images[0] 
                    : 'https://images.unsplash.com/photo-1551650975-87deedd944c3?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80';
                
                projectCard.innerHTML = `
                    <div class="project-image">
                        <img src="${imageUrl}" alt="${project.title}" loading="lazy">
                    </div>
                    <div class="project-content">
                        <h3>${project.title}</h3>
                        <span class="project-category">${categoryText}</span>
                        <p>${project.description.substring(0, 100)}...</p>
                        <div style="margin-top: 15px;">
                            ${project.features && project.features.length > 0 
                                ? `<small><strong>Recursos:</strong> ${project.features.slice(0, 2).join(', ')}</small>` 
                                : ''}
                        </div>
                    </div>
                `;
                
                projectsGrid.appendChild(projectCard);
            });
        }

        // Manipular envio do formulário
        document.addEventListener('DOMContentLoaded', function() {
            // Menu mobile
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const navLinks = document.getElementById('navLinks');
            
            if (mobileMenuBtn) {
                mobileMenuBtn.addEventListener('click', function() {
                    navLinks.classList.toggle('active');
                });
            }
            
            // Fechar menu ao clicar em um link
            document.querySelectorAll('.nav-links a').forEach(link => {
                link.addEventListener('click', function() {
                    navLinks.classList.remove('active');
                });
            });
            
            // Formulário de contato
            const contactForm = document.getElementById('contactForm');
            const contactSuccess = document.getElementById('contactSuccess');
            const contactError = document.getElementById('contactError');
            const submitBtn = document.getElementById('submitBtn');
            const submitText = document.getElementById('submitText');
            const submitLoading = document.getElementById('submitLoading');
            
            if (contactForm) {
                contactForm.addEventListener('submit', async function(e) {
                    e.preventDefault();
                    
                    // Validação básica
                    const name = document.getElementById('contactName').value.trim();
                    const email = document.getElementById('contactEmail').value.trim();
                    const subject = document.getElementById('contactSubject').value;
                    const message = document.getElementById('contactMessage').value.trim();
                    
                    if (!name || !email || !subject || !message) {
                        contactError.textContent = "Por favor, preencha todos os campos obrigatórios.";
                        contactError.style.display = 'block';
                        return;
                    }
                    
                    // Validar email
                    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                    if (!emailRegex.test(email)) {
                        contactError.textContent = "Por favor, insira um email válido.";
                        contactError.style.display = 'block';
                        return;
                    }
                    
                    // Mostrar loading
                    submitText.style.display = 'none';
                    submitLoading.style.display = 'inline';
                    submitBtn.disabled = true;
                    
                    // Coletar dados do formulário
                    const contactData = {
                        name: name,
                        email: email,
                        phone: document.getElementById('contactPhone').value.trim() || '',
                        subject: subject,
                        message: message,
                        date: new Date().toLocaleString('pt-BR'),
                        page: window.location.href
                    };
                    
                    // Salvar no Firebase
                    const result = await saveContactToFirebase(contactData);
                    
                    if (result.success) {
                        // Sucesso
                        contactSuccess.style.display = 'block';
                        contactError.style.display = 'none';
                        
                        // Limpar formulário
                        contactForm.reset();
                        
                        // Esconder mensagem após 5 segundos
                        setTimeout(() => {
                            contactSuccess.style.display = 'none';
                        }, 5000);
                        
                    } else {
                        // Erro
                        contactError.textContent = "Erro ao enviar mensagem. Por favor, tente novamente mais tarde.";
                        contactError.style.display = 'block';
                        contactSuccess.style.display = 'none';
                    }
                    
                    // Restaurar botão
                    submitText.style.display = 'inline';
                    submitLoading.style.display = 'none';
                    submitBtn.disabled = false;
                });
            }
            
            // Função para tratar erro na logo
            window.handleLogoError = function(element) {
                element.style.display = 'none';
                const logoText = element.nextElementSibling;
                if (logoText && logoText.id === 'logoText') {
                    logoText.style.display = 'flex';
                }
            };
            
            // Carregar informações da empresa
            function loadCompanyInfo() {
                // Nome da empresa
                const savedName = localStorage.getItem('bemorhCompanyName');
                if (savedName) {
                    document.getElementById('companyNameDisplay').textContent = savedName;
                    document.querySelectorAll('.logo-main').forEach(el => el.textContent = savedName);
                }
                
                // Tagline
                const savedTagline = localStorage.getItem('bemorhCompanyTagline');
                if (savedTagline) {
                    document.getElementById('companyTaglineDisplay').textContent = savedTagline;
                }
                
                // Descrição sobre
                const savedAbout = localStorage.getItem('bemorhCompanyAbout');
                if (savedAbout) {
                    document.getElementById('companyAboutDisplay').textContent = savedAbout;
                }
                
                // Contato
                const savedEmail = localStorage.getItem('bemorhCompanyEmail');
                const savedPhone = localStorage.getItem('bemorhCompanyPhone');
                const savedAddress = localStorage.getItem('bemorhCompanyAddress');
                
                if (savedEmail) document.getElementById('companyEmailDisplay').textContent = savedEmail;
                if (savedPhone) document.getElementById('companyPhoneDisplay').textContent = savedPhone;
                if (savedAddress) document.getElementById('companyAddressDisplay').textContent = savedAddress;
                
                // Logo
                const customLogo = localStorage.getItem('bemorhLogo');
                if (customLogo) {
                    const logoImage = document.getElementById('logoImage');
                    if (logoImage) {
                        logoImage.src = customLogo;
                    }
                }
            }
            
            // Carregar dados iniciais
            loadCompanyInfo();
            loadProjectsFromFirebase();
            
            // Atualizar contadores
            function updateCounters() {
                const projects = JSON.parse(localStorage.getItem('bemorhSiteProjects') || '[]');
                const activeProjects = projects.filter(p => p.status === 'active').length;
                
                const clients = JSON.parse(localStorage.getItem('bemorhClients') || '[]');
                const activeClients = clients.length;
                
                document.getElementById('projectCountDisplay').textContent = activeProjects > 0 ? `${activeProjects}+` : '20+';
                document.getElementById('clientCountDisplay').textContent = activeClients > 0 ? `${activeClients}+` : '15+';
            }
            
            updateCounters();
            
            // Smooth scroll para âncoras
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    
                    const targetId = this.getAttribute('href');
                    if (targetId === '#') return;
                    
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>
